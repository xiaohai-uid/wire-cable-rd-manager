# 线材研发管理系统 — 领域术语表

> 本文档定义项目中的领域术语，确保所有文档、代码、沟通使用统一的语言。

## 核心实体

### 产品 (Product)
研发的线材成品型号。每个产品有唯一的型号和对应的图纸编号，是串联所有文档的核心标识。

### BOM (Bill of Materials / 物料清单)
产品所需全部物料的清单。包含物料编码、名称、规格、材质、用量、损耗率、供应商等信息。

### BOM 明细 (BOM Item)
BOM 中的单条物料记录。一个 BOM 包含多条明细。

### 测试台账 (Test Ledger / Test Record)
日常测试数据的录入和管理。每条记录包含一个测试项目及其三次测试值，系统自动计算平均值和判定结果。

### 测试项目 (Test Item)
具体的测试指标，如导体电阻、绝缘耐压、外径尺寸、伸长率、老化测试等。

### 打样工艺记录 (Process Record)
每批次打样的工艺参数记录。包含设备信息、各工序参数、首件检验结果。

### 工艺参数 (Process Parameter)
某个工序的具体参数，如绞距、壁厚、外径、成缆节距等。

### 测试报告 (Test Report)
基于测试台账生成的正式报告文档。包含产品信息、测试条件、测试数据表格、综合结论、审批签名。

## 核心概念

### 判定 (Judgment)
基于测试值和规格要求的对比结果。可能值：合格、不合格。

### 不良率 (Defect Rate)
不合格记录数占总测试记录数的比率。公式：`COUNTIF(不合格) / COUNTA(全部)`

### 规格 (Specification / Spec)
测试项目的技术要求。包括三种类型：
- **上限规格**：值 ≤ 某阈值（如导体电阻 ≤0.5Ω）
- **下限规格**：值 ≥ 某阈值（如绝缘耐压 ≥1000V）
- **范围规格**：值在某个范围内（如外径 5.0±0.1mm）
- **定性规格**：通过/不通过（如外观检查）

### 批号/炉号 (Batch No)
生产批次的唯一标识。用于关联同一批次的测试记录和测试报告。

### 图纸编号 (Drawing No)
产品图纸的编号。与 BOM 编号、测试报告编号形成三角对应关系。

## 编号规范

| 实体 | 格式 | 示例 |
|------|------|------|
| 产品型号 | {系列}-{规格} | XH-100P-28AWG |
| 图纸编号 | DRG-{型号}-V{版本} | DRG-XH-100P-V1.0 |
| BOM 编号 | BOM-{型号}-V{版本} | BOM-XH-100P-V1.0 |
| 测试报告编号 | TR-{型号}-{批号}-V{版本} | TR-XH-100P-B001-V1.0 |
| 工艺记录编号 | GY-{型号}-{批号} | GY-XH-100P-B001 |

## 关联关系

- **产品** ↔ **BOM**：一个产品有一个或多个版本的 BOM
- **产品** ↔ **测试台账**：一个产品有多条测试记录
- **产品** ↔ **测试报告**：一个产品有多份测试报告
- **产品** ↔ **工艺记录**：一个产品有多条工艺记录
- **图纸编号** 和 **BOM 编号**：一个 BOM 对应一份图纸
- **批号** 串联：同一批号的测试台账和测试报告关联