# 线材研发管理系统 — 领域术语表

> 本文档定义项目中的领域术语，确保所有文档、代码、沟通使用统一的语言。

## 核心实体

### 产品 (Product)
研发的线材成品型号。每个产品有唯一的型号和对应的图纸编号，是串联所有文档的核心标识。

### BOM (Bill of Materials / 物料清单)
产品所需全部物料的清单。包含物料编码、名称、规格、材质、用量、损耗率、供应商等信息。

### BOM 明细 (BOM Item)
BOM 中的单条物料记录。一个 BOM 包含多条明细。

### 测试台账 (Test Ledger / Test Record)
日常测试数据的录入和管理。每条记录包含一个测试项目及其三次测试值，系统自动计算平均值和判定结果。

### 测试项目 (Test Item)
具体的测试指标，如导体电阻、绝缘耐压、外径尺寸、伸长率、老化测试等。

### 测试模板 (Test Template)
某个产品**应当测哪些项、每项规格是什么**的预定义清单。
它是批次录入的起点：选定产品后，模板决定网格里铺出多少行、每行的规格是什么。
例：RVV-3x1.5 的模板有 12 项，SYV-75-5 有 10 项。

### 批次录入 (Batch Entry)
以「产品 + 批号」为单位，一次性录入该批次全部测试项的操作。
一个批次的数据天然是二维表（测试项 × 三次测量），因此界面形态是网格而非表单（见 ADR 0003）。

### 打样工艺记录 (Process Record)
每批次打样的工艺参数记录。包含设备信息、各工序参数、首件检验结果。

### 工艺参数 (Process Parameter)
某个工序的具体参数，如绞距、壁厚、外径、成缆节距等。

### 测试报告 (Test Report)
基于测试台账生成的正式报告文档。包含产品信息、测试条件、测试数据表格、综合结论、审批签名。

## 核心概念

### 判定 (Judgment)
基于测试值和规格要求的对比结果。可能值：合格、不合格。

### 不良率 (Defect Rate)
不合格记录数占总测试记录数的比率。公式：`COUNTIF(不合格) / COUNTA(全部)`

### 规格 (Specification / Spec)
测试项目的技术要求，由人以字符串形式录入（如 `≤0.5Ω`、`2.8±0.2mm`）。
**规格是原始输入，不是系统内部使用的形式** —— 它必须在边界处解析成「判定规则」（见下）。

### 判定规则 (Judgment Rule)
规格字符串解析后得到的、带类型的结构化规则。系统内部只使用它，不再见到裸规格串。五种形态：

| 形态 | 含义 | 规格示例 |
|---|---|---|
| `max` | 值 ≤ 阈值 | `≤0.5Ω` |
| `min` | 值 ≥ 阈值 | `≥1500V` |
| `tolerance` | 中心值 ± 公差 | `2.8±0.2mm` |
| `range` | 上下界之间 | `5.0~5.5mm` |
| `qualitative` | 定性通过/不通过 | `合格` |

解析失败时**必须显式报错**，不允许猜测式回退 —— 对质检系统而言，
「静默给出错误判定」比「拒绝判定」严重得多（见 ADR 0004）。

### 数据端口 (DataPort)
UI 与数据来源之间的接口。有两个实现：
- **HttpAdapter** —— 走 HTTP 到本地服务与 SQLite，用于真实干活，数据落盘
- **MemoryAdapter** —— 内存 + localStorage，带真实种子数据，用于在线演示

领域逻辑（判定、规格解析、不良率统计）是纯函数，两个适配器共用（见 ADR 0002）。

### 批号/炉号 (Batch No)
生产批次的唯一标识。用于关联同一批次的测试记录和测试报告。

### 图纸编号 (Drawing No)
产品图纸的编号。与 BOM 编号、测试报告编号形成三角对应关系。

## 编号规范

| 实体 | 格式 | 示例 |
|------|------|------|
| 产品型号 | {系列}-{规格} | XH-100P-28AWG |
| 图纸编号 | DRG-{型号}-V{版本} | DRG-XH-100P-V1.0 |
| BOM 编号 | BOM-{型号}-V{版本} | BOM-XH-100P-V1.0 |
| 测试报告编号 | TR-{型号}-{批号}-V{版本} | TR-XH-100P-B001-V1.0 |
| 工艺记录编号 | GY-{型号}-{批号} | GY-XH-100P-B001 |

## 关联关系

- **产品** ↔ **BOM**：一个产品有一个或多个版本的 BOM
- **产品** ↔ **测试台账**：一个产品有多条测试记录
- **产品** ↔ **测试报告**：一个产品有多份测试报告
- **产品** ↔ **工艺记录**：一个产品有多条工艺记录
- **图纸编号** 和 **BOM 编号**：一个 BOM 对应一份图纸
- **批号** 串联：同一批号的测试台账和测试报告关联